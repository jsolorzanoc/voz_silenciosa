project_id = "lavozsilenciosa"

[functions.score-assessment]
# El puntaje solo se calcula para usuarios autenticados
verify_jwt = true

[functions.delete-account]
# Solo el propio usuario autenticado puede eliminar su cuenta
verify_jwt = true
